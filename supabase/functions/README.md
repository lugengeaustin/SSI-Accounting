# Edge functions (shared project hmiewhvemxrzhlkmhfpp)
Deployed 2026-07-27 during Supabase Move 1 (accounting → finance schema).
These are the LIVE sources: schema pinned to `finance`, receipt-intake's retired
2026-06-13 token removed. bulk-import (dead 410 stub) intentionally not ported.
Secrets: INTAKE_TOKENS + REPORT_TOKEN are function secrets in the dashboard.
